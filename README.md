
# Fashion Recommendation System using CNN (ResNet)

Overview

This project is a Fashion Recommendation System built using Convolutional Neural Networks (CNN) and ResNet architecture. It recommends visually similar fashion items based on an input image by extracting deep features and comparing them.

The system leverages transfer learning with a pretrained ResNet model to generate embeddings for fashion images and uses similarity metrics to find the closest matches
